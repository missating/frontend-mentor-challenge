import React from "react";

import Header from "components/Header";

const Home = props => {
  console.log(props, "home");

  return (
    <div>
      <Header />
    </div>
  );
};

export default Home;
